<h2>archives</h2>
<p>In reverse chronological order...
    <ul>
      <% if archives.length > 0 %>
        <% for entry in archives %>
          <li>
            <a href="<%= entry.path %>"><%= entry.title %>
                <span class="archiveInfo"><%= entry.date %></span>
            </a>
          </li>
        <% end %>
      <% end %>
    </ul>
</p>